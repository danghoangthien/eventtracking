<li class="nav-header">
	<div class="dropdown profile-element"> 
		<div class="user-panel">
			<span class="image">
				<img alt="image" class="img-circle" src="{{ auth_image_url }}">
			</span>
		</div>
		<a data-toggle="dropdown" class="dropdown-toggle" href="#">
			<span class="clear"> 
				<span class="block m-t-xs"> 
					<strong class="font-bold">{{ auth.name }}</strong>
				 </span> 
				 <small>Member since {{ auth.created|date("F Y") }}</small>
				 <span class="text-muted text-xs block">
				 	{{welcome_text}}
					<b class="caret"></b>
				</span> 
			</span> 
		</a>
		<ul class="dropdown-menu animated fadeInRight m-t-xs">
			<li><a href="{{ path('dashboard_show_App_Account', {id: auth.id, 'client_name': auth.clientId}) }}">Edit Profile</a></li>
			<li><a href="{{ path('dashboard_logout') }}">Sign Out</a></li>
		</ul>
	</div>
	<div class="logo-element">
		<span class="logo-mini"><img src="{{ asset('bundles/hyperevent/img/hypergrowth_small_transparent_16x35.png') }}"></span>
	</div>
</li>